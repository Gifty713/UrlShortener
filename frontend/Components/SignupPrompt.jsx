import { Button, DialogActions, Typography } from '@mui/material'; import Modal from './Modal'
function SignupPrompt({onClose,navigate}) { const dismiss=()=>{localStorage.removeItem('userId');onClose()}; return <Modal title="Get more from every link" onClose={dismiss}><Typography color="text.secondary">Create a free account to unlock QR code generation, click analytics, and password protection for your links.</Typography><DialogActions sx={{px:0,pt:2}}><Button onClick={dismiss}>Maybe later</Button><Button variant="contained" onClick={()=>navigate('/signup')}>Sign up free</Button></DialogActions></Modal> }; export default SignupPrompt
