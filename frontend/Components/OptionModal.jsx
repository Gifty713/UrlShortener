import { useState } from 'react'; import { Button, DialogActions, TextField, Typography } from '@mui/material'; import Modal from './Modal'
function OptionModal({type,initial,onSave,onClose}) { const [value,setValue]=useState(initial); const alias=type==='alias'; return <Modal title={alias?'Choose a custom alias':'Set an expiry date'} onClose={onClose}><Typography color="text.secondary" sx={{mb:2}}>{alias?'Make your link memorable with a custom ending.':'Your shortened link will stop working after this date.'}</Typography><form onSubmit={e=>{e.preventDefault();onSave(value.trim());onClose()}}><TextField autoFocus fullWidth label={alias?'Custom aliass':''} type={alias?'text':'date'} value={value} onChange={e=>setValue(e.target.value)} InputLabelProps={alias?undefined:{shrink:true}}/><DialogActions sx={{px:0,pt:2}}><Button onClick={onClose}>Cancel</Button><Button variant="contained" type="submit">Save</Button></DialogActions></form></Modal> }; export default OptionModal
